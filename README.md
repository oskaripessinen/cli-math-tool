# CLI Math Tools (C++)

A command-line calculator and function solver written in modern C++20.

Tools:
- calc   → evaluates expressions (2+5*(3-1))
- solve  → solves f(x)=0 in interval [a,b]

Build:

mkdir build
cd build
cmake ..
make
