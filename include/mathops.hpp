#ifndef MATHOPS_HPP
#define MATHOPS_HPP

#include <vector>
#include <string>

using std::string;
using std::vector;

void mult_div(vector<string> &nums);
void add_subs(vector<string> &nums);
void solve(vector<string> &nums);
#endif